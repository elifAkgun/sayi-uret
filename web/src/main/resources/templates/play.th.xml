<thlogic>
    <attr sel="#headId" th:replace="~{fragments/head:: headFragment}"></attr>
    <attr sel="#mainMessage" th:text="${__${T(code.elif.util.AttributeNames).MAIN_MESSAGE}__}"></attr>
    <attr sel="#resultMessage" th:text="${__${T(code.elif.util.AttributeNames).RESULT_MESSAGE}__}"></attr>
    <attr sel="#gameForm" th:action="@{__${T(code.elif.util.Views).PLAY}__}"></attr>
    <attr sel="#footer"  th:replace="~{fragments/footer::footerFragment}"></attr>

    <attr sel="#game-title"  th:text="#{game.title}"></attr>
    <attr sel="#enter-guess" th:text="#{game.enter.guess}"/>
    <attr sel="#submit" th:value="#{game.submit}"/>
</thlogic>